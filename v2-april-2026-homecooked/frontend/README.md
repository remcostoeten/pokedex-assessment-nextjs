# Frontend

For :the back-end to work run `dotnet run` `../PokedexAPI/`. Alternative for a work around set an enviorment variable in `.env` or `.env.local` with `NODE_ENV=production`. This will trick Next.js into thinking you're running production code which will skip the database and uses in browser json for the api.

Furthermore see [ASSESSMENT.md](./ASSESSMENT.md).
